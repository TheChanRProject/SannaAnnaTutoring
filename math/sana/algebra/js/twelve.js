function expFunc(x) {
  theFunc = Math.pow(3, -x)
  console.log(theFunc);
}
expFunc(-0.5)
expFunc(0.48)

function polyFunc(x) {
  theFunc = Math.pow(x, 3) + x
  console.log(theFunc);
}
polyFunc(-0.5)
polyFunc(0.48)
